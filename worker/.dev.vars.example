# Local-only development values. Copy to .dev.vars; never commit the real file.
TURNSTILE_SECRET_KEY=use-a-separate-local-or-cloudflare-test-secret
RATE_LIMIT_SALT=generate-a-random-value-of-at-least-32-characters
